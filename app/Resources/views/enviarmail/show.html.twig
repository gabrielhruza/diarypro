{% extends 'base.html.twig' %}

{% block body %}
    <h1>EnviarMail</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enviarMail.id }}</td>
            </tr>
            <tr>
                <th>De</th>
                <td>{{ enviarMail.de }}</td>
            </tr>
            <tr>
                <th>Para</th>
                <td>{{ enviarMail.para }}</td>
            </tr>
            <tr>
                <th>Asunto</th>
                <td>{{ enviarMail.asunto }}</td>
            </tr>
            <tr>
                <th>Cuerpo</th>
                <td>{{ enviarMail.cuerpo }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('enviarmail_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('enviarmail_edit', { 'id': enviarMail.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
